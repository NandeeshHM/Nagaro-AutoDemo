[Change_001 7th Feb 2026]
#Create a Python environment 
   python3 -m venv venv
#Activate the created environment 
   source venv/bin/activate

#This will install Playwright
   pip install playwright
#Now check the playwright version
   playwright --version
#Now we need to install all the browser available in plywright
   playwright install 

   <a class> This is called as link element 
